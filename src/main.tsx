import React,{useMemo,useState}from'react';import{createRoot}from'react-dom/client';import{invoke}from'@tauri-apps/api/core';import{open}from'@tauri-apps/plugin-dialog';import{Activity,AlertTriangle,Box,ChevronRight,Code2,FolderOpen,GitBranch,Layers3,Network,Search,ShieldAlert,Sparkles,Timer,XCircle}from'lucide-react';import'./styles.css';
type SymbolInfo={name:string;kind:string;line:number|null};type ImportRef={raw:string;target:string|null;external:boolean};type FileReport={path:string;language:string;bytes:number;lines:number|null;kind:string;role:string;confidence:number;purpose:string;symbols:SymbolInfo[];imports:ImportRef[];exports:string[];metrics:{functions:number;classes:number;comments:number;todo_count:number;max_line_length:number;estimated_complexity:number};warnings:string[]};type Report={name:string;root:string;summary:{files:number;directories:number;source_files:number;total_bytes:number;largest_file:string|null;detected_frameworks:string[];confidence:number};languages:{language:string;files:number;bytes:number}[];files:FileReport[];modules:{path:string;incoming:number;outgoing:number;role:string;centrality:number}[];edges:{from:string;to:string;kind:string}[];architecture:{title:string;detail:string;severity:string}[];warnings:string[]};
const bytes=(n:number)=>n<1024?`${n} B`:n<1024**2?`${(n/1024).toFixed(1)} KB`:n<1024**3?`${(n/1024**2).toFixed(1)} MB`:`${(n/1024**3).toFixed(1)} GB`;const base=(p:string)=>p.split('/').pop()||p;
function App(){const[report,setReport]=useState<Report|null>(null),[selected,setSelected]=useState<FileReport|null>(null),[q,setQ]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const filtered=useMemo(()=>report?report.files.filter(f=>`${f.path} ${f.role} ${f.language}`.toLowerCase().includes(q.toLowerCase())):[],[report,q]);async function scan(){setError('');const p=await open({directory:true,multiple:false,title:'Select repository'});if(!p||Array.isArray(p))return;setBusy(true);try{const r=await invoke<Report>('analyze',{path:p});setReport(r);setSelected(r.files.find(f=>f.kind==='source')??r.files[0]??null)}catch(e){setError(String(e))}finally{setBusy(false)}}if(!report)return <div className="landing"><div className="brand"><div className="mark">वि</div><div><b>Vimarśa</b><small>Repository intelligence, locally.</small></div></div><div className="hero"><div className="eyebrow"><Sparkles size={14}/> LOCAL · PRIVATE · STATIC ANALYSIS</div><h1>Understand the codebase you <span>did not write.</span></h1><p>Select the root of any project. Vimarśa recursively maps its structure, code roles, symbols and relationships without uploading your source.</p><button className="primary" onClick={scan}><FolderOpen size={18}/> Select repository</button>{busy&&<div className="status"><Activity size={15}/> Scanning the whole repository locally…</div>}{error&&<div className="error"><XCircle size={15}/>{error}</div>}</div><div className="features">{[['Recursive','Scans the complete repository root.'],['Explainable','Shows the evidence behind each inference.'],['Offline','No source upload or AI API is required.'],['Fast','Rust performs the filesystem analysis.']].map(x=><div key={x[0]}><b>{x[0]}</b><span>{x[1]}</span></div>)}</div></div>;return <div className="app"><header><div className="mini"><span className="mark sm">वि</span><b>Vimarśa</b></div><div className="path"><GitBranch size={14}/>{report.root}</div><button className="ghost" onClick={scan}><FolderOpen size={15}/> Open another</button></header><div className="layout"><aside><div className="section"><label>REPOSITORY</label><h3>{report.name}</h3>{[['Files',report.summary.files,Box],['Source',report.summary.source_files,Layers3],['Edges',report.edges.length,Network],['Size',bytes(report.summary.total_bytes),Timer]].map(([n,v,I])=><div className="stat" key={String(n)}><span><I size={14}/>{n}</span><b>{v}</b></div>)}</div><div className="section"><label>LANGUAGES</label>{report.languages.filter(x=>x.language!=='Other').slice(0,10).map(l=><div className="line" key={l.language}><span>{l.language}</span><span>{l.files}</span></div>)}</div><div className="section"><label>STACK</label>{report.summary.detected_frameworks.length?report.summary.detected_frameworks.map(x=><span className="chip" key={x}>{x}</span>):<div className="muted">No framework confidently detected.</div>}</div></aside><main><section className="overview"><div><div className="eyebrow"><Code2 size={14}/> REPOSITORY OVERVIEW</div><h2>{report.name}</h2><p>{report.summary.source_files} source files · {report.summary.directories} directories · {report.edges.length} local dependency edges</p></div><div className="conf"><b>{report.summary.confidence}%</b><span>analysis confidence</span></div></section><section className="insights">{report.architecture.map((x,i)=><div className={`insight ${x.severity}`} key={i}>{x.severity==='warning'?<AlertTriangle size={16}/>:<Network size={16}/>}<div><b>{x.title}</b><p>{x.detail}</p></div></div>)}</section><section className="workspace"><div className="files"><div className="panelHead"><div><b>Codebase</b><small>{filtered.length} matching files</small></div><div className="search"><Search size={15}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search files or roles"/></div></div><div className="list">{filtered.map(f=><button className={`row ${selected?.path===f.path?'active':''}`} key={f.path} onClick={()=>setSelected(f)}><Code2 size={15}/><div><b>{base(f.path)}</b><small>{f.path}</small></div><span>{f.role}</span><em>{f.confidence}%</em><ChevronRight size={15}/></button>)}</div></div><div className="detail">{selected?<><div className="detailHead"><div><h3>{base(selected.path)}</h3><small>{selected.path}</small></div><div className="badges"><span>{selected.language}</span><span>{selected.role}</span></div></div><div className="purpose"><div className="eyebrow"><Sparkles size={13}/> WHAT THIS FILE DOES</div><p>{selected.purpose}</p></div><div className="cards"><div><label>SYMBOLS</label><strong>{selected.symbols.length}</strong>{selected.symbols.slice(0,6).map(s=><span key={s.name}><b>{s.name}</b><small>{s.kind}{s.line?` · L${s.line}`:''}</small></span>)}</div><div><label>DEPENDENCIES</label><strong>{selected.imports.length}</strong>{selected.imports.slice(0,6).map(s=><span key={s.raw}><b>{s.raw}</b><small>{s.external?'external':'local'}</small></span>)}</div><div><label>EXPORTS</label><strong>{selected.exports.length}</strong>{selected.exports.slice(0,6).map(s=><span key={s}>{s}</span>)}</div></div><div className="metrics">{[['Lines',selected.lines??'—'],['Functions',selected.metrics.functions],['Classes',selected.metrics.classes],['Branch signals',selected.metrics.estimated_complexity],['Work markers',selected.metrics.todo_count]].map(x=><div key={x[0]}><b>{x[1]}</b><span>{x[0]}</span></div>)}</div>{selected.warnings.length>0&&<div className="warnings"><div className="eyebrow"><ShieldAlert size={13}/> REVIEW FLAGS</div>{selected.warnings.map(w=><p key={w}><AlertTriangle size={14}/>{w}</p>)}</div>}<div className="barWrap"><div><span>Inference confidence</span><b>{selected.confidence}%</b></div><i><u style={{width:`${selected.confidence}%`}}/></i></div></>:<div className="empty">Select a file to inspect it.</div>}</div></section></main></div></div>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
